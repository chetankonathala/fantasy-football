---
phase: 01-data-foundation
plan: 01
subsystem: database
tags: [sqlalchemy, alembic, sqlite, upsert, pytest, uv, nflreadpy, fastapi]

# Dependency graph
requires: []
provides:
  - SQLAlchemy ORM models (Player, Matchup) with full column set and constraints
  - SQLite upsert helpers (upsert_players, upsert_matchups) via insert().on_conflict_do_update()
  - Alembic configured with render_as_batch=True and initial migration applied
  - pytest infrastructure with in-memory SQLite fixtures (db_engine, db_session)
  - Project scaffolding: uv pyproject.toml, .gitignore, src/fantasy package structure
affects:
  - 01-data-foundation (plans 02-03 depend on these models and session fixtures)
  - 02-recommendation-engine (reads Player/Matchup models via SQLAlchemy)
  - 03-full-stack-core (FastAPI routes query DB via models installed here)

# Tech tracking
tech-stack:
  added:
    - nflreadpy==0.1.5
    - sqlalchemy==2.0.48
    - alembic==1.18.4
    - fastapi==0.135.1
    - requests>=2.32.5
    - pytest>=9.0.2 (dev)
  patterns:
    - TDD (RED-GREEN): tests written before implementation, confirmed failing then passing
    - SQLite upsert via insert().on_conflict_do_update() — never full-wipe
    - Alembic render_as_batch=True for SQLite ALTER TABLE compatibility
    - In-memory SQLite fixtures in conftest.py for fast, isolated unit tests

key-files:
  created:
    - src/fantasy/db/base.py
    - src/fantasy/db/models.py
    - src/fantasy/db/upsert.py
    - src/fantasy/normalize.py
    - src/fantasy/fetch/__init__.py
    - alembic/env.py
    - alembic/versions/f17c787e8d86_initial_schema.py
    - tests/conftest.py
    - tests/test_models.py
    - tests/test_upsert.py
    - pyproject.toml
    - alembic.ini
    - .gitignore
  modified: []

key-decisions:
  - "Matchup model defined before Player model in models.py to satisfy FK declaration order"
  - "Both nflverse_id (canonical) and sleeper_id stored on Player; FK join never on name strings"
  - "Week1-4 snap/target/carry share stored as explicit Float columns (not JSON) for direct SQL query access"
  - "updated_at uses Python lambda default/onupdate to avoid UTC timezone import issues with SQLAlchemy"
  - "uv used as package manager; uv.lock added to .gitignore per CONTEXT.md decision"

patterns-established:
  - "Pattern: In-memory SQLite via conftest.py fixtures — all subsequent test files use db_session"
  - "Pattern: Upsert by canonical ID — upsert_players on nflverse_id, upsert_matchups on (week, team, position)"
  - "Pattern: Alembic autogenerate with render_as_batch=True — required for all SQLite migrations"
  - "Pattern: src/fantasy/ package layout — db/, fetch/, normalize.py for clear separation of concerns"

requirements-completed: [DATA-01, DATA-03]

# Metrics
duration: 4min
completed: 2026-03-22
---

# Phase 01 Plan 01: Data Foundation Scaffolding Summary

**SQLAlchemy ORM models for Player (28 columns) and Matchup, SQLite upsert helpers with conflict resolution, Alembic batch-mode migration, and pytest in-memory SQLite fixtures — 11 tests green**

## Performance

- **Duration:** ~4 min
- **Started:** 2026-03-22T22:02:18Z
- **Completed:** 2026-03-22T22:06:18Z
- **Tasks:** 2 of 2
- **Files modified:** 18 created

## Accomplishments
- Player ORM model with 28 columns: canonical IDs (nflverse_id, sleeper_id), injury fields, 12 usage stat week columns (snap/target/carry share x4), matchup FK, and updated_at
- Matchup ORM model with week/team/position unique constraint and dvp_score/opponent_rank fields
- upsert_players and upsert_matchups helpers using SQLite insert().on_conflict_do_update() — atomic, no race conditions
- Alembic configured with render_as_batch=True and initial schema migration applied to data/fantasy.db
- pytest conftest.py with shared db_engine and db_session in-memory fixtures that all subsequent test files reuse

## Task Commits

Each task was committed atomically:

1. **Task 1: Project scaffolding, ORM models, and Alembic** - `65be7eb` (feat)
2. **Task 2: SQLite upsert helpers and test fixtures** - `633f93e` (feat)

_Note: Both tasks used TDD (RED-GREEN): tests written first, confirmed failing, then implementation written to pass them._

## Files Created/Modified
- `src/fantasy/db/base.py` - SQLAlchemy DeclarativeBase, get_engine(), get_session_factory()
- `src/fantasy/db/models.py` - Player and Matchup ORM models with all columns and constraints
- `src/fantasy/db/upsert.py` - upsert_players() and upsert_matchups() using on_conflict_do_update()
- `src/fantasy/normalize.py` - Placeholder for Phase 1 Plan 02 normalization layer
- `src/fantasy/fetch/__init__.py` - Placeholder package init
- `alembic/env.py` - Alembic environment with render_as_batch=True, imports Base.metadata
- `alembic/versions/f17c787e8d86_initial_schema.py` - Initial schema migration (autogenerated)
- `tests/conftest.py` - Shared db_engine and db_session fixtures (sqlite:///:memory:)
- `tests/test_models.py` - 5 model column/constraint tests
- `tests/test_upsert.py` - 6 upsert insert/update/empty-list tests
- `pyproject.toml` - uv project config with all dependencies and pytest ini_options
- `alembic.ini` - Alembic config pointing to sqlite:///data/fantasy.db
- `.gitignore` - data/*.db, logs/*.log, .venv/, uv.lock entries

## Decisions Made
- Matchup model declared before Player in models.py to satisfy SQLAlchemy FK forward-reference requirements
- Both nflverse_id and sleeper_id stored on Player for crosswalk lookups without string-based joins
- Week1-4 usage stats stored as explicit Float columns for direct SQL querying rather than JSON blobs
- updated_at uses Python lambda for default/onupdate to ensure timezone-aware UTC datetimes
- src/__init__.py added (not in plan) as required for Python package resolution in tests

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Added src/__init__.py missing from plan file list**
- **Found during:** Task 1 (test collection)
- **Issue:** Python test imports `from src.fantasy.db.base import Base` failed without src/__init__.py
- **Fix:** Created empty src/__init__.py to make src a proper Python package
- **Files modified:** src/__init__.py
- **Verification:** Test collection succeeded after adding the file
- **Committed in:** 65be7eb (Task 1 commit)

---

**Total deviations:** 1 auto-fixed (1 blocking)
**Impact on plan:** Required for test imports to resolve. No scope creep.

## Issues Encountered
- uv not installed in system PATH at execution start — installed via `pip3 install uv` before project initialization. No plan changes required.

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- Player and Matchup ORM models are the locked contract for Phase 1 Plan 02 (data ingestion) and Phase 2 (recommendation engine)
- tests/conftest.py fixtures available for all subsequent test files
- alembic/versions/ has the initial migration; future schema changes should use `alembic revision --autogenerate`
- data/fantasy.db is gitignored; the Alembic migration creates it on first `alembic upgrade head`

---
*Phase: 01-data-foundation*
*Completed: 2026-03-22*
