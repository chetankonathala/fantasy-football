---
phase: 03-full-stack-core
plan: 04
type: execute
wave: 4
depends_on: [03-01, 03-02, 03-03]
files_modified: []
autonomous: false
requirements: [SRCH-01, SRCH-02, RECD-04, RECD-05, DATA-02]

must_haves:
  truths:
    - "Full end-to-end flow works: search player -> select -> see recommendation card with all signals"
    - "Scoring format selector updates verdict without page reload"
    - "All visual elements match the UI-SPEC contract"
  artifacts: []
  key_links: []
---

<objective>
Human verification of the complete Phase 3 end-to-end flow: search, navigation, recommendation card, format switching, and visual correctness.

Purpose: Confirms all five Phase 3 success criteria are met before the phase is marked complete.
Output: User approval or issue list for gap closure.
</objective>

<execution_context>
@/Users/chetankonathala/.claude/get-shit-done/workflows/execute-plan.md
@/Users/chetankonathala/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-full-stack-core/03-CONTEXT.md
@.planning/phases/03-full-stack-core/03-UI-SPEC.md
@.planning/phases/03-full-stack-core/03-01-SUMMARY.md
@.planning/phases/03-full-stack-core/03-02-SUMMARY.md
@.planning/phases/03-full-stack-core/03-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Start both servers and run automated verification</name>
  <files></files>
  <read_first>
    - main.py (FastAPI app entry point)
    - frontend/package.json (Next.js scripts)
    - .planning/phases/03-full-stack-core/03-01-SUMMARY.md (API route details)
  </read_first>
  <action>
    **Step 1 — Run the full backend test suite:**

    ```bash
    cd /Users/chetankonathala/Desktop/fantasy-football
    uv run pytest tests/ -v
    ```

    All tests must pass. If any fail, fix before proceeding.

    **Step 2 — Start FastAPI backend:**

    ```bash
    cd /Users/chetankonathala/Desktop/fantasy-football
    uv run uvicorn main:app --reload --port 8000 &
    ```

    Verify API is responding:
    ```bash
    curl -s http://localhost:8000/search?q=test | head -c 200
    ```

    **Step 3 — Start Next.js frontend:**

    ```bash
    cd /Users/chetankonathala/Desktop/fantasy-football/frontend
    npm run dev &
    ```

    Wait for "Ready" message, then verify:
    ```bash
    curl -s http://localhost:3000 | head -c 200
    ```

    **Step 4 — Report URLs to user for verification.**
  </action>
  <verify>
    <automated>uv run pytest tests/ -v</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/ -v` exits 0
    - FastAPI server responds on http://localhost:8000
    - Next.js dev server responds on http://localhost:3000
  </acceptance_criteria>
  <done>Both servers running, full test suite passes, ready for human verification.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of complete Phase 3 end-to-end flow</name>
  <files></files>
  <action>
    Present the verification checklist to the user and wait for approval. The user must open http://localhost:3000 and verify the following:

    **1. Home Page (SRCH-01)**
    - Page has dark background (#0A0A0A)
    - "Fantasy Advisor" heading centered with "Start/Sit clarity, instantly." subtitle
    - Search bar centered, placeholder "Search players..."

    **2. Search Autocomplete (SRCH-01)**
    - Type 2+ characters of a player name (e.g., "Mah" for Mahomes)
    - Autocomplete dropdown appears with up to 5 suggestions
    - Each suggestion shows: name -- position, team
    - If no match, "No players found" message appears
    - Keyboard arrows navigate suggestions, Enter selects, Escape closes

    **3. Player Page Navigation (SRCH-02)**
    - Clicking a suggestion navigates to /player/[id]
    - Player name, position, and team shown in header
    - Nav header at top with "Fantasy Advisor" link and search bar

    **4. Recommendation Card (SRCH-02, RECD-04, RECD-05)**
    - Verdict (START/SIT/FLEX) is large, bold, and color-coded (green/red/amber)
    - Score shown next to verdict
    - "Why" section with 3-5 reasoning factors
    - "Signals" section with: Matchup Grade, Injury Status (colored badge), Practice Status, Snap % (L4W), Target/Carry Share (L4W), Projected Points

    **5. Scoring Format Selector (SRCH-02)**
    - Three pills: PPR / Half PPR / Standard
    - Active pill has eagles-green background
    - Changing format updates verdict and score WITHOUT page reload

    **6. Data Freshness (DATA-02)**
    - "Updated {relative time}" shown at bottom of card
    - Time is human-readable (e.g., "Updated 45 minutes ago")

    **7. Eagles Theme**
    - Dark background throughout
    - Midnight green accents on focus/active states
    - Silver secondary text

    NOTE: If the database has no player data (offseason), the user may need to run:
    ```bash
    cd /Users/chetankonathala/Desktop/fantasy-football
    uv run python scripts/refresh.py
    ```
  </action>
  <verify>User confirms "approved" or provides issue list</verify>
  <acceptance_criteria>
    - User confirms all 7 checklist sections pass
    - Or user provides specific issue descriptions for gap closure
  </acceptance_criteria>
  <done>User approves the complete Phase 3 end-to-end flow, confirming all ROADMAP success criteria are met.</done>
</task>

</tasks>

<verification>
- Full pytest suite passes
- Both servers respond
- User verifies all 7 checklist sections
</verification>

<success_criteria>
- User confirms all Phase 3 success criteria from ROADMAP.md:
  1. Search autocomplete works with partial player names
  2. Player page shows recommendation with verdict and reasoning
  3. Injury/practice status and usage trends visible
  4. Data freshness timestamp displayed
  5. Scoring format selector updates verdict without page reload
</success_criteria>

<output>
After completion, create `.planning/phases/03-full-stack-core/03-04-SUMMARY.md`
</output>
