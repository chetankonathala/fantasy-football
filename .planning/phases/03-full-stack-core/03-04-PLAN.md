---
phase: 03-full-stack-core
plan: 04
type: execute
wave: 3
depends_on: [03-01, 03-02, 03-03]
files_modified: []
autonomous: false
requirements: [SRCH-01, SRCH-02, RECD-04, RECD-05, DATA-02]

must_haves:
  truths:
    - "Backend starts on port 8000 and responds to /search and /player/{id}"
    - "Frontend starts on port 3000 and renders home page"
    - "Search autocomplete works end-to-end (type name, see results, click to navigate)"
    - "Player detail page shows verdict, reasoning, signals, injury status, usage trends, freshness"
    - "Scoring format selector changes verdict without page reload"
    - "All backend tests pass"
    - "Frontend builds clean"
  artifacts: []
  key_links:
    - from: "frontend (port 3000)"
      to: "backend (port 8000)"
      via: "CORS-enabled fetch calls"
      pattern: "localhost:8000"
---

<objective>
End-to-end verification: start both servers, run the full test suite, and have the user visually verify the complete search-to-recommendation flow.

Purpose: Confirms all Phase 3 requirements (SRCH-01, SRCH-02, RECD-04, RECD-05, DATA-02) work together as a system.
Output: Verified working system with both servers running.
</objective>

<execution_context>
@/Users/chetankonathala/.claude/get-shit-done/workflows/execute-plan.md
@/Users/chetankonathala/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-full-stack-core/03-01-SUMMARY.md
@.planning/phases/03-full-stack-core/03-02-SUMMARY.md
@.planning/phases/03-full-stack-core/03-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full backend test suite and start both servers</name>
  <files></files>
  <read_first>
    - main.py (verify FastAPI app is configured)
    - frontend/package.json (verify scripts available)
  </read_first>
  <action>
**Step 1: Run full backend test suite:**
```bash
cd /Users/chetankonathala/Desktop/fantasy-football
uv run pytest tests/ -v
```
All tests must pass. If any fail, fix them before proceeding.

**Step 2: Build frontend:**
```bash
cd /Users/chetankonathala/Desktop/fantasy-football/frontend
npm run build
```
Build must succeed with no errors.

**Step 3: Start backend server** (in background):
```bash
cd /Users/chetankonathala/Desktop/fantasy-football
uv run uvicorn main:app --host 0.0.0.0 --port 8000 &
```

**Step 4: Verify backend is responding:**
```bash
curl -s http://localhost:8000/search?q=test | head -c 200
```
Should return JSON (empty array is fine if no test data in DB).

**Step 5: Start frontend server** (in background):
```bash
cd /Users/chetankonathala/Desktop/fantasy-football/frontend
npm run dev &
```

**Step 6: Verify frontend is responding:**
```bash
curl -s http://localhost:3000 | head -c 500
```
Should return HTML containing "Fantasy Advisor".

Report both server statuses to the user before the checkpoint.
  </action>
  <verify>
    <automated>cd /Users/chetankonathala/Desktop/fantasy-football && uv run pytest tests/ -v 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/ -v` exits 0 with all tests passing
    - `cd frontend && npm run build` exits 0
    - Backend responds on port 8000 (curl returns JSON)
    - Frontend responds on port 3000 (curl returns HTML)
  </acceptance_criteria>
  <done>Both servers running. All backend tests pass. Frontend builds clean.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human visual verification of end-to-end flow</name>
  <files></files>
  <action>
Present the user with verification instructions. Both servers must be running (from Task 1). The user will visually verify the complete search-to-recommendation flow in their browser.
  </action>
  <what-built>Complete Phase 3 full-stack: FastAPI backend with search and player recommendation endpoints, Next.js frontend with Eagles dark theme, SearchBar autocomplete, and RecommendationCard with verdict, reasoning, signals, format selector, and freshness timestamp.</what-built>
  <how-to-verify>
    1. Open http://localhost:3000 in your browser
    2. Verify: Dark background (#0A0A0A), centered "Fantasy Advisor" heading, subtitle "Start/Sit clarity, instantly.", search bar visible
    3. Type a player name (3+ chars) into the search bar — verify autocomplete dropdown appears with results formatted as "Name — POS, TEAM"
    4. Click a player result — verify navigation to /player/[id] page
    5. On player page, verify:
       a. Player name and "POS · TEAM" header visible
       b. Verdict (START/SIT/FLEX) is large, bold, and color-coded (green/red/amber)
       c. Score displayed next to verdict
       d. "Why" section with 3-5 reasoning factors as bullet list
       e. "Signals" section with: Injury Status (colored badge), Practice Status, Snap % (L4W), Target/Carry Share (L4W), Projected Points
       f. Scoring format pills (PPR / Half PPR / Standard) — click each, verdict updates without page reload
       g. "Updated X minutes ago" freshness timestamp at bottom
    6. Verify nav header: "Fantasy Advisor" link on left, search bar on right — search from any page
    7. (If no data in DB): The search will return empty results — verify "No players found" message appears. If you want full demo, run `uv run python scripts/refresh.py` first to populate the database.

    NOTE: If the NFL season is not active, the refresh script may not populate data. In that case, verify the UI structure and API responses (search returns empty, player page shows error state for non-existent IDs).
  </how-to-verify>
  <verify>Human visual inspection of the running application</verify>
  <acceptance_criteria>
    - User confirms home page renders with dark theme and centered search
    - User confirms search autocomplete works (or shows "No players found" with no data)
    - User confirms player detail page shows recommendation card with all signals
    - User confirms format selector updates verdict without reload
  </acceptance_criteria>
  <done>User has visually verified the end-to-end flow and approved Phase 3.</done>
  <resume-signal>Type "approved" if the full-stack flow works correctly, or describe any issues you see.</resume-signal>
</task>

</tasks>

<verification>
- All backend tests pass: `uv run pytest tests/ -v`
- Frontend builds: `cd frontend && npm run build`
- End-to-end: search -> select -> recommendation card renders with all signals
- Format selector updates verdict without page reload
- Human visual verification of Eagles theme, layout, accessibility
</verification>

<success_criteria>
- Both servers start and communicate via CORS
- Search-to-recommendation flow works end-to-end
- All 5 Phase 3 requirements verified: SRCH-01 (search autocomplete), SRCH-02 (player detail), RECD-04 (injury status), RECD-05 (usage trends), DATA-02 (freshness timestamp)
- User approves visual and functional quality
</success_criteria>

<output>
After completion, create `.planning/phases/03-full-stack-core/03-04-SUMMARY.md`
</output>
